import React from 'react';
import { Grid } from '@material-ui/core';

const DirectorateView = ({ }) => {
  return (
    <div className='container'>
      <Grid container spacing={3}>
        <Grid item xs={6}>
        </Grid>
      </Grid>
    </div>
  );
}

export default DirectorateView;